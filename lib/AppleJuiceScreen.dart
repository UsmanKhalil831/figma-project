import 'package:flutter/material.dart';

class AppleJuice extends StatelessWidget {
  const AppleJuice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.purple,child:const Center(child:Text('apple juice screen')),)
    );
  }
}