import 'package:flutter/material.dart';

class FreshLemon extends StatelessWidget {
  const FreshLemon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.purple,child:const Center(child:Text('fresh lemon screen')),)
    );
  }
}