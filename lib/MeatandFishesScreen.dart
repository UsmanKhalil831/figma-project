import 'package:flutter/material.dart';

class MeatnFishScreen extends StatelessWidget {
  const MeatnFishScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.blueGrey,child: const Center(child: Text('Fish and Meats Screen'),),)
    );
  }
}