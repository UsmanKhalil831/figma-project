import 'package:flutter/material.dart';

class LowPrice extends StatelessWidget {
  const LowPrice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.purple,child:const Center(child:Text('Low Price screen')),)
    );
  }
}