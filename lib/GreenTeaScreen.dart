import 'package:flutter/material.dart';

class GreenTea extends StatelessWidget {
  const GreenTea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.purple,child:const Center(child:Text('green tea screen')),)
    );
  }
}