import 'package:flutter/material.dart';

class SmallFishes extends StatelessWidget {
  const SmallFishes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.purple,child:const Center(child:Text('Small fishes screen')),)
    );
  }
}